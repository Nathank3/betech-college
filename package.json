{
  "name": "betech-college-landing",
  "version": "1.0.0",
  "description": "Modern landing page for Betech Computer College",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"npx nodemon server.js\" \"npx browser-sync start --proxy localhost:8001 --files 'templates/**/*.ejs, static/**/*.*' --reload-delay 500 --no-open\""
  },
  "dependencies": {
    "compression": "^1.8.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.11"
  }
}
